{
  "name": "resumark-browser-check",
  "private": true,
  "scripts": {
    "build:web": "cd apps/web && trunk build --release --locked",
    "test:web": "npm run build:web && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
