{
  "name": "resumark-browser-check",
  "private": true,
  "scripts": {
    "dev:web": "cd apps/web && sh ../../scripts/rustup-exec.sh trunk serve --open --locked",
    "build:web": "cd apps/web && sh ../../scripts/rustup-exec.sh trunk build --release --locked",
    "test:web": "npm run build:web && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
